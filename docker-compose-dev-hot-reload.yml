version: '3'

services:
  # Organization service with hot reload
  organization:
    depends_on:
      - nats
      - postgres
    build:
      context: ./
      dockerfile: Dockerfiles/Dockerfile.organization.dev
    env_file:
      - ./.env
    volumes:
      # Mount entire source code for hot reloading
      - ./apps/organization:/app/apps/organization
      - ./apps/user:/app/apps/user
      - ./libs:/app/libs
      - ./package.json:/app/package.json
      - ./nest-cli.json:/app/nest-cli.json
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.build.json:/app/tsconfig.build.json
      # Mount compiled dist for immediate changes
      - ./dist:/app/dist
      # Exclude node_modules from host
      - /app/node_modules
    # Let the Dockerfile startup script handle Prisma generation and startup
    environment:
      - NODE_ENV=development
      - WATCH_MODE=true
    networks:
      - confirmd_network

  # API Gateway with hot reload
  api-gateway:
    depends_on:
      - nats
      - postgres
      - redis
    build:
      context: ./
      dockerfile: Dockerfiles/Dockerfile.api-gateway.dev
    expose:
      - '5000'
    env_file:
      - ./.env
    volumes:
      # API Gateway specific files
      - ./apps/api-gateway/src:/app/apps/api-gateway/src
      - ./apps/api-gateway/common:/app/apps/api-gateway/common
      - ./apps/api-gateway/tsconfig.app.json:/app/apps/api-gateway/tsconfig.app.json
      # Other apps directories needed for imports
      - ./apps/connection:/app/apps/connection
      - ./apps/organization:/app/apps/organization
      - ./apps/user:/app/apps/user
      - ./apps/issuance:/app/apps/issuance
      - ./apps/ledger:/app/apps/ledger
      - ./apps/webhook:/app/apps/webhook
      # Shared libraries and config
      - ./libs:/app/libs
      - ./package.json:/app/package.json
      - ./nest-cli.json:/app/nest-cli.json
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.build.json:/app/tsconfig.build.json
      - ./dist:/app/dist
      - /app/node_modules
    command: ['pnpm', 'run', 'start:dev', 'api-gateway']
    environment:
      - NODE_ENV=development
      - WATCH_MODE=true
      - REDIS_HOST=redis
    networks:
      - confirmd_network

  # User service with hot reload
  user:
    depends_on:
      - nats
      - api-gateway
    build:
      context: ./
      dockerfile: Dockerfiles/Dockerfile.user.dev
    env_file:
      - ./.env
    volumes:
      # Mount entire source code for hot reloading
      - ./apps/user:/app/apps/user
      - ./libs:/app/libs
      - ./package.json:/app/package.json
      - ./nest-cli.json:/app/nest-cli.json
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.build.json:/app/tsconfig.build.json
      # Mount compiled dist for immediate changes
      - ./dist:/app/dist
      # Exclude node_modules from host
      - /app/node_modules
    command: ['pnpm', 'run', 'start:dev', 'user'] # Use watch mode
    environment:
      - NODE_ENV=development
      - WATCH_MODE=true
    networks:
      - confirmd_network

  # Add a file watcher service for shared libraries
  lib-watcher:
    build:
      context: ./
      dockerfile: Dockerfiles/Dockerfile.lib-watcher
    volumes:
      - ./libs:/app/libs
      - ./dist/libs:/app/dist/libs
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.build.json:/app/tsconfig.build.json
    command: ['pnpm', 'run', 'build:libs:watch'] # Watch and rebuild libs
    environment:
      - NODE_ENV=development
    networks:
      - confirmd_network

  # Supporting services (from main docker-compose.yml)
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: confirmd
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    networks:
      - confirmd_network

  redis:
    image: redis:6.2-alpine
    restart: always
    ports:
      - '6379:6379'
    command: redis-server --save 20 1 --loglevel warning --appendonly yes
    volumes:
      - cache:/data
    networks:
      - confirmd_network
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 3s
      retries: 3

  nats:
    image: nats:2.9-alpine
    command: ['-c', '/etc/nats/nats-server.conf']
    volumes:
      - './nats-server.conf:/etc/nats/nats-server.conf'
    ports:
      - '4222:4222'
      - '8222:8222'
    networks:
      - confirmd_network

  # NGINX Reverse Proxy
  nginx-proxy:
    image: nginx:alpine
    ports:
      - '5000:80'
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./uploads:/app/uploads:ro
    depends_on:
      - api-gateway
    networks:
      - confirmd_network

  minio:
    image: minio/minio
    container_name: confirmd-platform-minio
    restart: always
    ports:
      - '9000:9000'
      - '9001:9001'
    environment:
      - MINIO_ROOT_USER=dev-access-key
      - MINIO_ROOT_PASSWORD=dev-secret-key
      - MINIO_SERVER_URL=https://minio.confamd.com
      - MINIO_BROWSER_REDIRECT_URL=https://minio-console.confamd.com
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - confirmd_network

  # Seed service
  seed:
    container_name: seed-service
    image: ghcr.io/credebl/seed:latest
    env_file:
      - ./.env
    volumes:
      - $PWD/libs/prisma-service/prisma/data/credebl-master-table.json:/app/libs/prisma-service/prisma/data/credebl-master-table.json
    networks:
      - confirmd_network

  # Utility service with hot reload and MinIO support
  utility:
    depends_on:
      - nats
      - api-gateway
      - minio
    build:
      context: ./
      dockerfile: Dockerfiles/Dockerfile.utility.dev
    env_file:
      - ./.env
    volumes:
      # Utility specific files
      - ./apps/utility/src:/app/apps/utility/src
      - ./apps/utility/tsconfig.app.json:/app/apps/utility/tsconfig.app.json
      # Shared libraries and config
      - ./libs:/app/libs
      - ./package.json:/app/package.json
      - ./nest-cli.json:/app/nest-cli.json
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.build.json:/app/tsconfig.build.json
      - ./dist:/app/dist
      - /app/node_modules
    command: ['pnpm', 'run', 'start:dev', 'utility']
    environment:
      - NODE_ENV=development
      - WATCH_MODE=true
      - AWS_ENDPOINT_URL=http://minio:9000
      - AWS_S3_STOREOBJECT_ACCESS_KEY=dev-access-key
      - AWS_S3_STOREOBJECT_SECRET_KEY=dev-secret-key
      - AWS_S3_STOREOBJECT_REGION=us-east-1
      - AWS_S3_STOREOBJECT_BUCKET=confirmd-dev-bucket
    networks:
      - confirmd_network

  # Connection service
  connection:
    depends_on:
      - nats
      - api-gateway
      - utility
      - user
    build:
      context: ./
      dockerfile: Dockerfiles/Dockerfile.connection
    env_file:
      - ./.env
    networks:
      - confirmd_network

  # Issuance service with hot reload and resend support
  issuance:
    depends_on:
      - nats
      - redis
      - api-gateway
      - user
      - connection
    build:
      context: ./
      dockerfile: Dockerfiles/Dockerfile.issuance.dev
    env_file:
      - ./.env
    volumes:
      # Issuance specific files
      - ./apps/issuance:/app/apps/issuance
      # Shared libraries and config
      - ./libs:/app/libs
      - ./package.json:/app/package.json
      - ./nest-cli.json:/app/nest-cli.json
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.build.json:/app/tsconfig.build.json
      # Mount compiled dist for immediate changes
      - ./dist:/app/dist
      # Exclude node_modules from host
      - /app/node_modules
    command: ['pnpm', 'run', 'start:dev', 'issuance']
    environment:
      - NODE_ENV=development
      - WATCH_MODE=true
      - REDIS_HOST=redis
    networks:
      - confirmd_network

  # Ledger service with hot reload
  ledger:
    depends_on:
      - nats
      - api-gateway
      - user
      - connection
      - issuance
    image: confirmd-ledger-dev
    env_file:
      - ./.env
    volumes:
      # Ledger specific files
      - ./apps/ledger/src:/app/apps/ledger/src
      - ./apps/ledger/tsconfig.app.json:/app/apps/ledger/tsconfig.app.json
      # Other apps directories needed for imports
      - ./apps/connection:/app/apps/connection
      - ./apps/organization:/app/apps/organization
      - ./apps/user:/app/apps/user
      - ./apps/issuance:/app/apps/issuance
      - ./apps/webhook:/app/apps/webhook
      # Shared libraries and config
      - ./libs:/app/libs
      - ./package.json:/app/package.json
      - ./nest-cli.json:/app/nest-cli.json
      - ./tsconfig.json:/app/tsconfig.json
      - ./tsconfig.build.json:/app/tsconfig.build.json
      - ./dist:/app/dist
      - /app/node_modules
    command: ['pnpm', 'run', 'start:dev', 'ledger']
    environment:
      - NODE_ENV=development
      - WATCH_MODE=true
    networks:
      - confirmd_network

  # Verification service
  verification:
    depends_on:
      - nats
      - api-gateway
      - user
      - connection
      - issuance
      - ledger
      - organization
    image: ghcr.io/credebl/verification:latest
    env_file:
      - ./.env
    networks:
      - confirmd_network

  # Agent provisioning service
  agent-provisioning:
    depends_on:
      - nats
      - api-gateway
      - user
      - connection
      - issuance
      - ledger
      - organization
      - verification
    image: ghcr.io/credebl/agent-provisioning:latest
    env_file:
      - ./.env
    environment:
      - ROOT_PATH=$PWD/apps/agent-provisioning/AFJ/agent-config
    volumes:
      - $PWD/apps/agent-provisioning/AFJ/agent-config:/app/agent-provisioning/AFJ/agent-config
      - /var/run/docker.sock:/var/run/docker.sock
      - $PWD/agent.env:/app/agent.env
    networks:
      - confirmd_network

  # Agent service
  agent-service:
    depends_on:
      - nats
      - api-gateway
      - user
      - connection
      - issuance
      - ledger
      - organization
      - verification
      - agent-provisioning
    command: sh -c 'until (docker logs confirmd-platform-agent-provisioning-1 | grep "Agent-Provisioning-Service Microservice is listening to NATS"); do sleep 1; done && node dist/apps/agent-service/main.js'
    build:
      context: .
      dockerfile: Dockerfiles/Dockerfile.agent-service
    env_file:
      - ./.env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    volumes_from:
      - agent-provisioning
    networks:
      - confirmd_network

  # Cloud wallet service
  cloud-wallet:
    depends_on:
      - nats
      - api-gateway
    image: ghcr.io/credebl/cloud-wallet:latest
    env_file:
      - ./.env
    networks:
      - confirmd_network

  # Geolocation service
  geolocation:
    depends_on:
      - nats
      - api-gateway
    image: ghcr.io/credebl/geolocation:latest
    env_file:
      - ./.env
    networks:
      - confirmd_network

  # Notification service
  notification:
    depends_on:
      - nats
      - redis
      - api-gateway
    image: ghcr.io/credebl/notification:latest
    env_file:
      - ./.env
    environment:
      - REDIS_HOST=redis
    networks:
      - confirmd_network

  # Webhook service
  webhook:
    depends_on:
      - nats
      - api-gateway
    image: ghcr.io/credebl/webhook:latest
    env_file:
      - ./.env
    networks:
      - confirmd_network

  # Schema file server
  schema-file-server:
    container_name: schema-file-server
    image: ghcr.io/credebl/schema-file-server:latest
    env_file:
      - ./.env
    networks:
      - confirmd_network

volumes:
  postgres_data:
  cache:
  minio_data:
    driver: local

networks:
  confirmd_network:
    driver: bridge
