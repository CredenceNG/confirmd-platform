The API Gateway should not be exposing ports directly since NGINX is handling the reverse proxy.

for dev use /Users/itopa/projects/confirmd-platform/docker-compose-dev-hot-reload.yml to build and run the containers
